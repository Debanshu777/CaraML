package com.debanshu777.caraml.core.theme

/**
 * User-controlled dark mode preference.
 *
 * [SYSTEM] follows the host OS setting; [LIGHT] and [DARK] override it.
 */
enum class ThemeMode {
    SYSTEM,
    LIGHT,
    DARK,
}
